# Runtime
PORT=4173

# Domain / API endpoints
GRUDGE_API_URL=https://api.grudge-studio.com
GRUDGE_AUTH_URL=https://id.grudge-studio.com
OBJECTSTORE_WORKER_URL=https://objectstore.grudge-studio.com
OBJECTSTORE_STATIC_URL=https://molochdagod.github.io/ObjectStore

# Puter AI server-side token (optional for /api/ai/chat route)
# If unset, browser-side Puter AI still works directly in Agent Playground.
PUTER_AUTH_TOKEN=

# Optional R2 write API key for objectstore worker (if your worker requires it)
OBJECTSTORE_API_KEY=
